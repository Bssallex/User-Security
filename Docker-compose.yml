version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: user-security
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - "5435:5432"
    volumes:
      - gadata:/var/lib/postgresql/data

volumes:
  gadata: